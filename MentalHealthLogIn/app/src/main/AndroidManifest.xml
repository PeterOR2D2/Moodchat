<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.mentalhealthlogin">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".AddNotification"></activity>
        <activity android:name=".AddFoodLog" />
        <activity android:name=".AddSleepLog" />
        <activity android:name=".AddMoodLog" />
        <activity android:name=".mood.ui.MoodHistoryActivity" />
        <activity android:name=".AddActivityLog" />
        <activity android:name=".Main3Activity" />
        <activity android:name=".DisplayActivityLog" />
        <activity android:name=".Home" />
        <activity android:name=".DisplaySleepActivity" />
        <activity android:name=".DisplayFoodLog" />
        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>


        <meta-data
            android:name="preloaded_fonts"
            android:resource="@array/preloaded_fonts" />
    </application>

</manifest>